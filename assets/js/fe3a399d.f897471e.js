"use strict";(self.webpackChunkmy_website=self.webpackChunkmy_website||[]).push([[8355],{3059:(e,t,n)=>{n.r(t),n.d(t,{assets:()=>o,contentTitle:()=>r,default:()=>d,frontMatter:()=>a,metadata:()=>c,toc:()=>h});var s=n(74848),i=n(28453);const a={sidebar_position:7},r="Building the Bouncer: A Hands-On Guide to Implementing a KDB/Q Gateway from Scratch",c={id:"tutorials/gateway",title:"Building the Bouncer: A Hands-On Guide to Implementing a KDB/Q Gateway from Scratch",description:"This tutorial picks up right where the Gateway Architecture Design post left off, taking the next step handling routing, access control, load balancing, aggregation, and shielding backend services from unnecessary complexity. In this hands-on tutorial, we will take those concepts and turn them into something concrete by building a gateway step by step, from scratch. If any of the architectural concepts are still a little hazy, take a quick detour back to the architecture post here, this walkthrough assumes you know why the gateway exists and focuses on how to make it real.",source:"@site/docs/tutorials/gateway.mdx",sourceDirName:"tutorials",slug:"/tutorials/gateway",permalink:"/docs/tutorials/gateway",draft:!1,unlisted:!1,tags:[],version:"current",sidebarPosition:7,frontMatter:{sidebar_position:7},sidebar:"tutorialSidebar",previous:{title:"Pulse Powered Alpha: Building your First Trading Strategy with KDB-X and Pulse",permalink:"/docs/tutorials/strategy"},next:{title:"Productivity Tools",permalink:"/docs/category/productivity-tools"}},o={},h=[{value:"From Blank Slate to Gateway: What We\u2019re About to Build",id:"from-blank-slate-to-gateway-what-were-about-to-build",level:2},{value:"The Life of a Query",id:"the-life-of-a-query",level:2},{value:"Where the Rubber Meets the Road: The Technical Walkthrough",id:"where-the-rubber-meets-the-road-the-technical-walkthrough",level:2},{value:"Gateway",id:"gateway",level:3},{value:"The <code>services</code> Table",id:"the-services-table",level:4},{value:"The <code>userQueries</code> Table",id:"the-userqueries-table",level:4},{value:"The <code>connectToLoadbalancer</code> Function",id:"the-connecttoloadbalancer-function",level:4},{value:"The <code>registerGatewayWithLoadbalancer</code> Function",id:"the-registergatewaywithloadbalancer-function",level:4},{value:"The <code>addService</code> Function",id:"the-addservice-function",level:4},{value:"The <code>getData</code> Function",id:"the-getdata-function",level:4},{value:"The <code>serviceAlloc</code> Function",id:"the-servicealloc-function",level:4},{value:"The <code>returnRes</code> Function",id:"the-returnres-function",level:4},{value:"The <code>.z.pc</code> Function",id:"the-zpc-function",level:4},{value:"The <code>.z.ts</code> Function",id:"the-zts-function",level:4},{value:"Loadbalancer",id:"loadbalancer",level:3},{value:"The <code>services</code> Table",id:"the-services-table-1",level:4},{value:"The <code>serviceQueue</code> Table",id:"the-servicequeue-table",level:4},{value:"The <code>gateways</code> List",id:"the-gateways-list",level:4},{value:"The <code>registerGW</code> Function",id:"the-registergw-function",level:4},{value:"The <code>registerService</code> Function",id:"the-registerservice-function",level:4},{value:"The <code>sendService</code> Function",id:"the-sendservice-function",level:4},{value:"The <code>requestService</code> Function",id:"the-requestservice-function",level:4},{value:"The <code>addQueryToQueue</code> Function",id:"the-addquerytoqueue-function",level:4},{value:"The <code>releaseService</code> Function",id:"the-releaseservice-function",level:4},{value:"The <code>serviceAvailable</code> Function",id:"the-serviceavailable-function",level:4},{value:"The <code>.z.pc</code> Function",id:"the-zpc-function-1",level:4},{value:"Backend Service",id:"backend-service",level:3},{value:"The <code>connectToLoadbalancer</code> Function",id:"the-connecttoloadbalancer-function-1",level:4},{value:"The <code>serviceDetails</code> List",id:"the-servicedetails-list",level:4},{value:"The <code>execQuery</code> Function",id:"the-execquery-function",level:4},{value:"The <code>queryService</code> Function",id:"the-queryservice-function",level:4},{value:"Backend Bootstrapping and Bureaucracy: Wiring the Service to the Grid",id:"backend-bootstrapping-and-bureaucracy-wiring-the-service-to-the-grid",level:4},{value:"Client Onboarding: Injecting Queries into the Machine",id:"client-onboarding-injecting-queries-into-the-machine",level:3},{value:"Lights On: Watching the Framework Come Alive",id:"lights-on-watching-the-framework-come-alive",level:2},{value:"Where Do We Go From Here? Scaling the Blueprint",id:"where-do-we-go-from-here-scaling-the-blueprint",level:2},{value:"Resources",id:"resources",level:2}];function l(e){const t={a:"a",code:"code",em:"em",h1:"h1",h2:"h2",h3:"h3",h4:"h4",img:"img",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,i.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(t.h1,{id:"building-the-bouncer-a-hands-on-guide-to-implementing-a-kdbq-gateway-from-scratch",children:"Building the Bouncer: A Hands-On Guide to Implementing a KDB/Q Gateway from Scratch"}),"\n",(0,s.jsxs)(t.p,{children:["This tutorial picks up right where the ",(0,s.jsx)(t.strong,{children:"Gateway Architecture Design"})," post left off, taking the next step: turning theory into working code. In the previous post, we looked at the gateway as the ",(0,s.jsx)(t.a,{href:"https://www.defconq.tech/docs/architecture/gateway",children:"The Bouncer at the Front Door of Your KDB/Q Stack"}),", deciding who gets in, where queries go, and how the system behaves under load. In other terms: handling routing, access control, load balancing, aggregation, and shielding backend services from unnecessary complexity. In this hands-on tutorial, we will take those concepts and turn them into something concrete by building a gateway ",(0,s.jsx)(t.strong,{children:"step by step, from scratch"}),". If any of the architectural concepts are still a little hazy, take a quick detour back to the architecture post ",(0,s.jsx)(t.a,{href:"https://www.defconq.tech/docs/architecture/gateway",children:"here"}),", this walkthrough assumes you know ",(0,s.jsx)(t.em,{children:(0,s.jsx)(t.strong,{children:"why"})})," the gateway exists and focuses on ",(0,s.jsx)(t.em,{children:(0,s.jsx)(t.strong,{children:"how"})})," to make it real."]}),"\n",(0,s.jsx)(t.h2,{id:"from-blank-slate-to-gateway-what-were-about-to-build",children:"From Blank Slate to Gateway: What We\u2019re About to Build"}),"\n",(0,s.jsxs)(t.p,{children:["In the following sections we will build the core pieces of a ",(0,s.jsx)(t.strong,{children:"scalable query routing framework"})," inspired by the ",(0,s.jsx)(t.a,{href:"https://code.kx.com/q/wp/query-routing/",children:(0,s.jsx)(t.strong,{children:"Query Routing"})})," from ",(0,s.jsx)(t.a,{href:"https://kx.com",children:"KX"})," white paper, written by ",(0,s.jsx)(t.a,{href:"https://www.linkedin.com/in/kevin-holsgrove-2b191158/",children:"Kevin Holsgrove"}),". The goal is to illustrate a practical, production-ready pattern where multiple processes coordinate to handle user queries efficiently in a distributed KDB/Q system."]}),"\n",(0,s.jsx)(t.p,{children:"But before we dive into the technical implementation of the framework, let\u2019s take a brief step back and look at the system from a higher-level perspective, focusing on the processes we\u2019re about to build and how they fit together. At the center of our design are three key players:"}),"\n",(0,s.jsxs)(t.ul,{children:["\n",(0,s.jsxs)(t.li,{children:["\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.strong,{children:"Gateway"}),": This process sits at the edge of your system, accepts client requests, annotates them with a unique sequence identifier, and hands them off for routing. Each incoming query is captured in an internal table, tagged with a unique sequence number, and linked back to the client\u2019s process handle. The gateway then asks the load balancer for an available service and forwards the query to whatever worker gets allocated. When the result comes back, the gateway uses the sequence number to match it to the original request and sends the response straight back to the right user."]}),"\n"]}),"\n",(0,s.jsxs)(t.li,{children:["\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.strong,{children:"Load Balancer / Connection Manager"}),": Think of this as the brains of query distribution. It knows what services are available (e.g., multiple HDBs, RDBs, or custom compute services), which ones are busy, and which are free to take work. Rather than simple round-robin, this component tags incoming queries and only allocates them to idle backends, minimizing queueing and improving latency."]}),"\n"]}),"\n",(0,s.jsxs)(t.li,{children:["\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.strong,{children:"Service Instances"}),": These are the actual workers: database processes, analytics engines, or custom report builders that evaluate user queries. Each one registers itself with the load balancer so it can be included in allocation decisions. Once a service finishes a request, it reports back to the load balancer so it can be assigned another job. By running multiple instances of the same service, for example, HDBs backed by identical data sets or RDBs subscribing to the same tickerplant, we create a shared pool of resources that can be spread across different servers. This opens the door to a hot-hot setup, where the framework can not only distribute load efficiently across available instances, but also seamlessly reroute user queries if a service or host goes down, without clients needing to be aware of the failure."]}),"\n"]}),"\n"]}),"\n",(0,s.jsx)(t.p,{children:(0,s.jsx)(t.img,{alt:"The mastermind behind the DefconQ Gateway Bouncer",src:n(82946).A+"",width:"1024",height:"1536"})}),"\n",(0,s.jsx)(t.h2,{id:"the-life-of-a-query",children:"The Life of a Query"}),"\n",(0,s.jsxs)(t.p,{children:["From a user\u2019s perspective, interaction is straightforward. Queries are sent to the gateway handle via a single entry point, the ",(0,s.jsx)(t.code,{children:"userQuery"})," function, which takes a two-element list: the target service and the query to execute. Communication with the gateway uses deferred synchronous messaging, giving users a familiar request\u2013response model while allowing the system to remain non-blocking under the hood."]}),"\n",(0,s.jsx)(t.p,{children:"Services backing the different database types can be distributed across multiple servers, with the number of instances driven by available hardware and user demand, and configured independently per service type. To keep the focus where it matters, we intentionally keep the gateway\u2019s routing logic simple here and put the spotlight on the load balancer\u2019s role in the system. The flow itself is equally clean. Once a query enters the system, the gateway coordinates with the load balancer to allocate an appropriate service, forwards the request, and waits for the result. Responses are then routed back to the user through the gateway. If something goes wrong, an invalid service request or an error during query evaluation, the gateway propagates a clear error back to the client, keeping failure handling centralized and predictable."}),"\n",(0,s.jsx)(t.p,{children:"The load balancer, instead of relying on a simple round-robin strategy, takes a more deliberate approach. It actively tracks which service instances are free and only allocates queries to resources that are actually available. Once a service finishes executing a query, it explicitly notifies the load balancer that it\u2019s ready to take on more work, allowing the system to maintain an accurate view of capacity at all times. The only exception to this flow occurs when a service has been allocated but the user disconnects before the query completes. In that case, the gateway steps in and informs the load balancer that the service is no longer needed, ensuring resources are released promptly and not left unnecessarily occupied."}),"\n",(0,s.jsx)(t.p,{children:"The diagram below shows the full lifecycle of a query as it moves through the system."}),"\n",(0,s.jsx)(t.p,{children:(0,s.jsx)(t.img,{alt:"DefconQ Gateway Tutorial",src:n(64892).A+""})}),"\n",(0,s.jsx)(t.h2,{id:"where-the-rubber-meets-the-road-the-technical-walkthrough",children:"Where the Rubber Meets the Road: The Technical Walkthrough"}),"\n",(0,s.jsxs)(t.p,{children:["In this section, we\u2019ll roll up our sleeves and walk through the technical implementation of each moving part in the system: the gateway, the load balancer, the backend services, and a client process sending queries through the gateway. We will first outline the key responsibilities and core functionality of each process, so it\u2019s clear what role they play and how they fit together. Once the concepts are in place, we\u2019ll look at how they\u2019re implemented in practice. The full codebase for this walkthrough is available in the DefconQ ",(0,s.jsx)(t.a,{href:"https://github.com/DefconQ/defconQ/tree/master/projects",children:"GitHub repository"}),", for anyone who wants to explore, run, or extend it themselves."]}),"\n",(0,s.jsx)(t.h3,{id:"gateway",children:"Gateway"}),"\n",(0,s.jsx)(t.p,{children:"At its core, the gateway maintains two tables, one for tracking available backend services and another for tracking incoming user queries, along with a small set of functions that coordinate communication between the client, the gateway, and the load balancer. In the following sections, we will break these pieces down and explain how they work together."}),"\n",(0,s.jsxs)(t.h4,{id:"the-services-table",children:["The ",(0,s.jsx)(t.code,{children:"services"})," Table"]}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.code,{children:"services"})," table keeps track of all backend services currently available to the gateway. It is implemented as a keyed table, with the backend service address as the key. For each entry, the table stores the service name as well as the corresponding connection handle. Note that this handle represents the IPC connection established from the gateway to the backend service, not the client connection."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"services:([address:()] serviceName:();sh:());\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-userqueries-table",children:["The ",(0,s.jsx)(t.code,{children:"userQueries"})," Table"]}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.code,{children:"userQueries"})," table records every query that enters the gateway. This is also a keyed table, using a unique sequence number as the key. The sequence number is a simple counter that increments with each new incoming query. Alongside it, the table captures the user handle that submitted the query, the timestamp when the query was recorded, the timestamp when it was forwarded to a backend service, the timestamp when the result was returned, the user name, the service handle and service name associated with the execution, and finally the query itself. Together, this table provides full visibility into the lifecycle of each request as it flows through the gateway."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"userQueries:([sq:`int$()] uh:`int$(); rec:`timestamp$(); snt:`timestamp$(); ret:`timestamp$(); user:`$(); sh:`int$(); serv:`$(); query:());\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-connecttoloadbalancer-function",children:["The ",(0,s.jsx)(t.code,{children:"connectToLoadbalancer"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.code,{children:"connectToLoadbalancer"})," function is responsible for establishing the IPC connection from the gateway to the load balancer. It opens the connection and stores both the original handle, a positive handle used for synchronous communication, and its negative counterpart for asynchronous messaging, which is how most interactions will be handled. If the connection attempt fails, the function simply reports an error."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"connectToLoadbalancer:{@[{NLB::neg LB::hopen x};`:localhost:1234;{show x}]};\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-registergatewaywithloadbalancer-function",children:["The ",(0,s.jsx)(t.code,{children:"registerGatewayWithLoadbalancer"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.code,{children:"registerGatewayWithLoadbalancer"})," function then performs the initial handshake with the load balancer. It sends a synchronous message invoking the ",(0,s.jsx)(t.code,{children:"registerGW"})," function on the load balancer. While we will explain the inner workings of the load balancer later, at a high level this call registers the gateway and returns the full set of backend services currently known to the load balancer. The gateway uses this response to populate its internal services table via the ",(0,s.jsx)(t.code,{children:"addService"})," function. This exchange is deliberately synchronous: just like a real-time subscriber registering with a tickerplant, the gateway must wait for this response before continuing its startup sequence."]}),"\n",(0,s.jsx)(t.p,{children:"If you\u2019d like to go deeper into IPC in KDB/Q or the mechanics of the tickerplant, check out the dedicated posts linked in the resources section at the end of this article."}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"registerGatewayWithLoadbalancer:{addService LB(`registerGW;`)};\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-addservice-function",children:["The ",(0,s.jsx)(t.code,{children:"addService"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.code,{children:"addService"})," function updates the gateway\u2019s internal service table with the details of all available backend services. As part of this process, it establishes a direct IPC connection from the gateway to each backend service and then upserts the corresponding entries into the services table with the relevant connection and service metadata."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"addService:{ `services upsert `address xkey update sh:{hopen first x}each address from x };\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-getdata-function",children:["The ",(0,s.jsx)(t.code,{children:"getData"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.code,{children:"getData"})," function is the main entry point into our KDB/Q system, the front door to the data warehouse, the ",(0,s.jsx)(t.em,{children:(0,s.jsx)(t.strong,{children:"\u201copen sesame\u201d"})})," to whatever insights are hiding behind your data points. It\u2019s also the first place where the gateway logic starts to get a little more interesting, so it\u2019s worth walking through it line by line."]}),"\n",(0,s.jsxs)(t.p,{children:["We begin with a simple conditional check that splits execution into two paths. First, we verify whether the service the user is trying to query actually exists. If it does, the gateway logs the query along with all its metadata, increments the global sequence number, and then asks the load balancer to allocate the next available backend service capable of handling the request. From there, the query continues its journey through the system. In the (hopefully rare) case where the requested service does ",(0,s.jsx)(t.em,{children:(0,s.jsx)(t.strong,{children:"not"})})," exist, the gateway immediately returns a clear error message to the client, and that's where this particular query\u2019s very short life comes to an end."]}),"\n",(0,s.jsxs)(t.p,{children:["Remember, the ",(0,s.jsx)(t.code,{children:"getData"})," function expects a tuple, a two-element list, where the first element specifies the name of the service to query, and the second element contains the actual query the user wants to execute."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:'getData:{\n  $[(serv:x 0) in exec distinct serviceName from services;\n    [userQueries,:(SEQ+:1;.z.w;.z.p;0Np;0Np;.z.u;0N;serv;x 1);\n      NLB(`requestService;SEQ;serv)];\n    (neg .z.w)(`$"Service Unavailable")] };\n'})}),"\n",(0,s.jsxs)(t.h4,{id:"the-servicealloc-function",children:["The ",(0,s.jsx)(t.code,{children:"serviceAlloc"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["The next function we look at is ",(0,s.jsx)(t.code,{children:"serviceAlloc"}),". This function is called by the load balancer once it has identified an available backend service and is returning its address to the gateway. At this point, the gateway once again uses a conditional check to determine whether the user who originally submitted the query is still around and waiting for a response, after all, client processes can disappear for any number of reasons."]}),"\n",(0,s.jsxs)(t.p,{children:["To make this determination, the gateway looks up the query in the ",(0,s.jsx)(t.code,{children:"userQueries"})," table, which tracks all active requests. This works because the sequence number assigned to the query was passed to the load balancer when the gateway initially requested a free service, and is now returned alongside the allocation. That sequence number lets the gateway reliably match the allocation back to the correct query."]}),"\n",(0,s.jsxs)(t.p,{children:["If the user is no longer waiting, there\u2019s nothing left to do. The gateway simply releases the backend service by calling ",(0,s.jsx)(t.code,{children:"releaseService"})," on the load balancer, freeing it up for other work. If the user ",(0,s.jsx)(t.em,{children:(0,s.jsx)(t.strong,{children:"is"})})," still waiting, the gateway sends an asynchronous message to the allocated backend service, invoking the ",(0,s.jsx)(t.code,{children:"queryService"})," function to kick off execution. Finally, the gateway updates the query\u2019s metadata in the ",(0,s.jsx)(t.code,{children:"userQueries"})," table and continues with the normal execution flow."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"serviceAlloc:{[sq;addr]\n  $[null userQueries[sq;`uh];\n    NLB(`releaseService;sq);\n    [(neg sh:services[addr;`sh]) (`queryService;(sq;userQueries[sq;`query]));\n    userQueries[sq;`snt`sh]:(.z.p;sh)]]};\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-returnres-function",children:["The ",(0,s.jsx)(t.code,{children:"returnRes"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["Next up is the function that closes the loop and delivers the result back to the client. The ",(0,s.jsx)(t.code,{children:"returnRes"})," function is called by the backend service once it has finished executing the user\u2019s query. It receives a tuple, a two element list, where the first element is the sequence number of the query and the second element is the actual result containing the requested data."]}),"\n",(0,s.jsxs)(t.p,{children:["Using the sequence number, the gateway looks up the corresponding entry in the ",(0,s.jsx)(t.code,{children:"userQueries"})," table and retrieves the handle of the waiting client. If the client is still connected, the gateway sends the result back to the user. As a final step, it updates the timestamp marking when the response was returned. With that, the lifecycle of this query comes to a close, and the gateway is ready to handle the next request."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"returnRes:{[res]\n  uh:first exec uh from userQueries where sq=(res 0);\n  if[not null uh;(neg uh)(res 1)];\n  userQueries[(res 0);`ret]:.z.p };\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-zpc-function",children:["The ",(0,s.jsx)(t.code,{children:".z.pc"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["One important edge case to cover is what happens when a connection to the gateway is dropped. We handle this by overriding the default behaviour of the internal system function ",(0,s.jsx)(t.code,{children:".z.pc"})," with gateway-specific logic. This allows us to clean up properly and ensure no KDB/Q process is left hanging indefinitely."]}),"\n",(0,s.jsxs)(t.p,{children:["The cleanup happens in stages. First, we null out the user handle for all entries in the ",(0,s.jsx)(t.code,{children:"userQueries"})," table where the handle matches the disconnected client. Next, we remove any backend services whose service handle corresponds to the dropped connection. Rather than branching with conditional checks, we simply delete matching rows, if there\u2019s no match, nothing happens."]}),"\n",(0,s.jsxs)(t.p,{children:["If the disconnected handle belongs to a backend service, we then identify all affected, still-waiting users and return an appropriate ",(0,s.jsx)(t.strong,{children:"\u201cService Disconnected\u201d"})," error message. Finally, we check whether the dropped connection was the load balancer itself. If so, the gateway broadcasts an error to all clients with outstanding queries, closes all backend service connections (since no further allocations are possible), clears the services table, and updates the sent and return timestamps for all in-flight queries."]}),"\n",(0,s.jsx)(t.p,{children:"As a last step, the gateway resets its load balancer state and arms a timer with a 10-second interval, attempting to re-establish the connection to the load balancer every ten seconds until it\u2019s back online."}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:'.z.pc:{[handle]\n  update uh:0N from `userQueries where uh=handle;\n  delete from `services where sh=handle;\n  if[count sq:exec distinct sq from userQueries where sh=handle,null ret;\n    returnRes\'[sq cross `$"Service Disconnect"]];\n  if[handle~LB;\n    (neg exec uh from userQueries where not null uh,null snt)@\\:`$"Service Unavailable";\n    hclose each (0!services)`sh;\n    delete from `services;\n    update snt:.z.p,ret:.z.p from `userQueries where not null uh,null snt;\n    LB::0; NLB::0; value"\\\\t 10000"] };\n'})}),"\n",(0,s.jsxs)(t.h4,{id:"the-zts-function",children:["The ",(0,s.jsx)(t.code,{children:".z.ts"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["Last but definitely not least, we wire up the function that should fire whenever the internal timer ticks by assigning it to the system hook ",(0,s.jsx)(t.code,{children:".z.ts"}),". On each trigger, the gateway attempts to open a connection to the load balancer and, if successful, proceeds to register itself by calling ",(0,s.jsx)(t.code,{children:"registerGatewayWithLoadbalancer"}),"."]}),"\n",(0,s.jsx)(t.p,{children:"To keep things robust, the whole sequence is wrapped in a trap, f anything goes wrong, we catch it and return a meaningful error instead of letting the gateway stumble forward in an undefined state. Better safe than sorry. Once the connection is established and the gateway is successfully registered, we shut the timer back down by resetting the interval to zero. At that point, the gateway, the load balancer, and the backend services are all in sync and fully operational."}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:'z.ts:{\n  connectToLoadbalancer[];\n  if[0<LB;@[registerGatewayWithLoadbalancer;`;{show x}];value"\\\\t 0"] };\n'})}),"\n",(0,s.jsx)(t.h3,{id:"loadbalancer",children:"Loadbalancer"}),"\n",(0,s.jsx)(t.p,{children:"Now that the gateway has done its job of funnelling user queries, it\u2019s time to meet the load balancer, the quiet workhorse that decides who actually does the heavy lifting. Think of it as mission control: it keeps a live view of available backend services, queues incoming requests from the gateway, and tracks every gateway currently plugged into the system. Backed by a small set of purpose-built tables and a handful of core functions, the load balancer is responsible for making sure work is assigned efficiently, safely, and at the right time. In the sections that follow, we\u2019ll break down these building blocks and walk through the logic that keeps everything moving."}),"\n",(0,s.jsxs)(t.h4,{id:"the-services-table-1",children:["The ",(0,s.jsx)(t.code,{children:"services"})," Table"]}),"\n",(0,s.jsx)(t.p,{children:"This table tracks all available service instances and the gateways currently consuming them. It is keyed by the backend service handle and records the service address, service name, the gateway handle currently assigned to the service, the sequence number of the query being processed, and the corresponding timestamp."}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"services:([handle:`int$()] address:`$(); serviceName:`$(); gwHandle:`int$(); sq:`int$(); udt:`timestamp$());\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-servicequeue-table",children:["The ",(0,s.jsx)(t.code,{children:"serviceQueue"})," Table"]}),"\n",(0,s.jsx)(t.p,{children:"The serviceQueue table keeps track of all requests waiting for an available resource. It is keyed by the gateway handle the request originated from and the corresponding service request sequence number. In addition, it records the target service name and the timestamp marking when the request reached the load balancer."}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"serviceQueue:([gwHandle:`int$();sq:`int$()] serviceName:`$(); time:`timestamp$());\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-gateways-list",children:["The ",(0,s.jsx)(t.code,{children:"gateways"})," List"]}),"\n",(0,s.jsx)(t.p,{children:"The load balancer also keeps a lightweight list of gateway handles, aptly named gateways, which tracks all gateways currently connected to it."}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"gateways:()\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-registergw-function",children:["The ",(0,s.jsx)(t.code,{children:"registerGW"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["To track which gateways are connected to the load balancer, we maintain a simple list of gateway handles. New entries are added via the ",(0,s.jsx)(t.code,{children:"registerGW"})," unction. When a gateway connects, this function records its handle and immediately responds with the full set of currently available backend services. Armed with this information, the gateway can populate and synchronise its internal service table with the service names and addresses known to the load balancer."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"registerGW:{gateways,:.z.w ; select serviceName, address from services};\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-registerservice-function",children:["The ",(0,s.jsx)(t.code,{children:"registerService"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["Gateways matter, but without backend services there\u2019s nothing to route to. To onboard a newly connected backend, we define the ",(0,s.jsx)(t.code,{children:"registerService"})," function. It takes the service name and address and upserts the entry into the load balancer\u2019s services table. Since this service is now part of the ecosystem, its details must be broadcast to all connected gateways. We do this using the each-left iterator, invoking ",(0,s.jsx)(t.code,{children:"addService"})," on every registered gateway handle with the relevant metadata. Once that propagation is complete, the service is marked as available and ready to take work."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"registerService:{[name;addr]\n  `services upsert (.z.w;addr;name;0N;0N;.z.p);\n  (neg gateways)@\\:(`addService;enlist`serviceName`address!(name;addr));\n  serviceAvailable[.z.w;name] };\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-sendservice-function",children:["The ",(0,s.jsx)(t.code,{children:"sendService"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["When a gateway asks the load balancer for an available backend, we need a clean way to hand that information back. That\u2019s the job of the ",(0,s.jsx)(t.code,{children:"sendService"})," function. It selects a free backend service, returns its handle and address to the gateway, and immediately invokes the gateway\u2019s ",(0,s.jsx)(t.code,{children:"serviceAlloc"})," function so the user query can be forwarded to its final destination."]}),"\n",(0,s.jsxs)(t.p,{children:["If you look closely at the implementation, you\u2019ll notice the use of ",(0,s.jsx)(t.a,{href:"https://code.kx.com/q/ref/raze/",children:(0,s.jsx)(t.code,{children:"raze"})})," when constructing the message sent back to the gateway. At first glance this might seem a little odd, but it\u2019s entirely intentional. Indexing into the services table by handle yields the sequence number and address, which we then combine with the symbolic name of ",(0,s.jsx)(t.code,{children:"serviceAlloc"}),"."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"q)services\nhandle| address   serviceName       gwHandle sq udt\n------| ---------------------------------------------------------------------\n7     | :mac:2222 EQUITY_MARKET_RDB             2026.02.10D22:13:43.941150000\nq)services[7;`sq`address]\n0Ni\n`:mac:2222\nq)(`serviceAlloc;services[7;`sq`address])\n`serviceAlloc\n(0Ni;`:mac:2222)\nq)0N!(`serviceAlloc;services[7;`sq`address])\n(`serviceAlloc;(0Ni;`:mac:2222))\n`serviceAlloc\n(0Ni;`:mac:2222)\n"})}),"\n",(0,s.jsxs)(t.p,{children:["Without ",(0,s.jsx)(t.code,{children:"raze"}),", this produces a two-element list: the function name and a tuple containing the arguments. That would cause ",(0,s.jsx)(t.code,{children:"serviceAlloc"})," to be called with a single list argument, which is not what we want. Since ",(0,s.jsx)(t.code,{children:"serviceAlloc"})," expects two atomic parameters, we flatten the structure with ",(0,s.jsx)(t.code,{children:"raze"}),", ensuring the function is invoked with the correct argument shape."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"q)raze (`serviceAlloc;services[7;`sq`address])\n`serviceAlloc\n0Ni\n`:mac:2222\nq)0N!raze (`serviceAlloc;services[7;`sq`address])\n(`serviceAlloc;0Ni;`:mac:2222)\n`serviceAlloc\n0Ni\n`:mac:2222\n"})}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.strong,{children:"Full implementation"}),":"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"sendService:{[gw;h]neg[gw]raze(`serviceAlloc;services[h;`sq`address])};\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-requestservice-function",children:["The ",(0,s.jsx)(t.code,{children:"requestService"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["Before we can dispatch a free backend to a gateway, we first need to receive and process the request. That responsibility sits with the ",(0,s.jsx)(t.code,{children:"requestService"})," function. It takes two parameters: the sequence number assigned by the gateway and the symbolic name of the backend service that should handle the user\u2019s query."]}),"\n",(0,s.jsxs)(t.p,{children:["The first step inside ",(0,s.jsx)(t.code,{children:"requestService"})," is resource discovery. We query the ",(0,s.jsx)(t.code,{children:"services"})," table for a row where the service name matches the requested service ",(0,s.jsx)(t.em,{children:(0,s.jsx)(t.strong,{children:"and"})})," the gateway handle column is null. In other words, a service instance that is currently idle and not allocated to another gateway. Availability is not assumed; it is explicitly verified."]}),"\n",(0,s.jsxs)(t.p,{children:["If a suitable service is found, we immediately log the request in the ",(0,s.jsx)(t.code,{children:"serviceQueue"})," table via ",(0,s.jsx)(t.code,{children:"addQueryToQueue"}),". This gives us traceability and ordering. We then update the ",(0,s.jsx)(t.code,{children:"services"})," table itself, assigning the backend instance to the requesting gateway, recording the query\u2019s sequence number, and stamping the allocation time. At this point, the resource is officially reserved."]}),"\n",(0,s.jsx)(t.p,{children:"With the bookkeeping done, we send the handle of the allocated backend service to the gateway. From there, the gateway takes over and forwards the client\u2019s query to its final destination, the backend service that will actually execute it.Clean allocation. Clear state."}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"requestService:{[seq;serv]\n  res:exec first handle from services where serviceName=serv,null gwHandle;\n  $[null res;\n    addQueryToQueue[seq;serv;.z.w];\n    [services[res;`gwHandle`sq`udt]:(.z.w;seq;.z.p);\n      sendService[.z.w;res]]] };\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-addquerytoqueue-function",children:["The ",(0,s.jsx)(t.code,{children:"addQueryToQueue"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["If an incoming request cannot be processed immediately, it needs to be parked in the ",(0,s.jsx)(t.code,{children:"serviceQueue"})," table. That\u2019s precisely the role of the ",(0,s.jsx)(t.code,{children:"addQueryToQueue"})," function. It takes the query\u2019s sequence number, the target service name, and the gateway handle from which the request originated, and inserts this information into the queue table. As part of the process, it also stamps the entry with the current timestamp, ensuring we retain a clear record of when the request entered the load balancer."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"addQueryToQueue:{[seq;serv;gw]`serviceQueue upsert (gw;seq;serv;.z.p)};\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-releaseservice-function",children:["The ",(0,s.jsx)(t.code,{children:"releaseService"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["Once a backend service has either completed a client request or the client has disconnected and no longer requires a response, the service instance must be freed and marked as available again. That responsibility falls to the ",(0,s.jsx)(t.code,{children:"releaseService"})," function."]}),"\n",(0,s.jsxs)(t.p,{children:["If ",(0,s.jsx)(t.code,{children:"releaseService"})," is triggered by a backend service, meaning the query was successfully processed, we simply call ",(0,s.jsx)(t.code,{children:"serviceAvailable"})," with the relevant service details, marking the instance as idle and ready for new work. If, on the other hand, ",(0,s.jsx)(t.code,{children:"releaseService"})," is invoked by a gateway, this signals that the client disconnected before the result was returned. In that case, we identify the backend service that was allocated to that query using the gateway handle and the query\u2019s service name, and then call ",(0,s.jsx)(t.code,{children:"serviceAvailable"})," with those details. Either way, the outcome is the same: the resource is returned to the pool and can be reassigned."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"releaseService:{\n  serviceAvailable . $[.z.w in (0!services)`handle;\n    (.z.w;x);\n    value first select handle,serviceName from services\n      where gwHandle=.z.w,sq=x ] };\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-serviceavailable-function",children:["The ",(0,s.jsx)(t.code,{children:"serviceAvailable"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.code,{children:"releaseService"})," function delegates some of the workload to the ",(0,s.jsx)(t.code,{children:"serviceAvailable"})," function. This function determines whether the freed resource should simply be marked as idle or immediately reassigned to the next waiting request. In doing so, it updates both the ",(0,s.jsx)(t.code,{children:"services"})," and ",(0,s.jsx)(t.code,{children:"serviceQueue"})," tables to reflect the new state."]}),"\n",(0,s.jsxs)(t.p,{children:["The first step is to retrieve all gateway handle and sequence number combinations from the ",(0,s.jsx)(t.code,{children:"serviceQueue"})," where the service name matches the backend service being released. Thanks to KDB/Q's left-of-right evaluation efficiency, we can directly extract the first gateway/sequence pair from that result set, effectively identifying the next request in line."]}),"\n",(0,s.jsxs)(t.p,{children:["We then remove that entry from the ",(0,s.jsx)(t.code,{children:"serviceQueue"})," and update the ",(0,s.jsx)(t.code,{children:"services"})," table with the allocation details. If a queued request exists, the backend service is immediately reassigned, and its handle is sent to the corresponding gateway so processing can continue without delay. If no matching requests remain, the execution flow stops there. This means the service is simply marked as available and waits for the next allocation."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"serviceAvailable:{[zw;serv]\n  nxt:first n:select gwHandle,sq from serviceQueue where serviceName=serv;\n  serviceQueue::(1#n)_ serviceQueue;\n  services[zw;`gwHandle`sq`udt]:(nxt`gwHandle;nxt`sq;.z.p);\n  if[count n; sendService[nxt`gwHandle;zw]] };\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-zpc-function-1",children:["The ",(0,s.jsx)(t.code,{children:".z.pc"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["The final piece on the load balancer side mirrors the gateway\u2019s approach: we override the system function ",(0,s.jsx)(t.code,{children:".z.pc"})," to handle disconnections cleanly and deterministically."]}),"\n",(0,s.jsxs)(t.p,{children:["When a process disconnects, the first step is to remove its handle from the ",(0,s.jsx)(t.code,{children:"services"})," table. If the disconnected handle does not belong to a backend service, the table remains unchanged, no branching logic required. We then apply the same logic to the list of registered gateways, removing the handle if it belongs to a gateway; otherwise, the list stays as is."]}),"\n",(0,s.jsxs)(t.p,{children:["Next, we clean up the ",(0,s.jsx)(t.code,{children:"serviceQueue"})," by deleting any outstanding requests submitted by the disconnected gateway. If the originating gateway is no longer alive, there is no client to receive the result, making those queued queries obsolete.\nFinally, we check whether any backend service is currently allocated to the gateway that just dropped. If so, we release that resource by resetting the occupying gateway handle to null. A disconnected gateway cannot hold onto a backend service, and the system must reflect that immediately."]}),"\n",(0,s.jsx)(t.p,{children:"And with that, the load balancer is fully implemented, its responsibilities defined, its edge cases handled. The routing layer is now operational, and we can shift our focus to building a simple backend service to complete the architecture."}),"\n",(0,s.jsx)(t.h3,{id:"backend-service",children:"Backend Service"}),"\n",(0,s.jsxs)(t.p,{children:["In the next section, we\u2019ll walk through the implementation of a simple backend service. For the purpose of this tutorial, we\u2019ll assume the backend represents an equity real-time database (RDB) exposing two dummy tables: ",(0,s.jsx)(t.code,{children:"trade"})," and ",(0,s.jsx)(t.code,{children:"quote"}),". Alongside the data itself, we\u2019ll store the service name and the connection details required to register with the load balancer."]}),"\n",(0,s.jsxs)(t.p,{children:["At the core of the backend sits the ",(0,s.jsx)(t.code,{children:"execQuery"})," function, responsible for executing incoming client queries. However, to prevent a malformed or poorly written query from destabilising the service, we don\u2019t expose ",(0,s.jsx)(t.code,{children:"execQuery"})," directly. Instead, it is wrapped inside ",(0,s.jsx)(t.code,{children:"queryService"}),", where we use a trap to safely execute the request and contain potential errors."]}),"\n",(0,s.jsx)(t.p,{children:"Let\u2019s break down each of these components in detail."}),"\n",(0,s.jsxs)(t.h4,{id:"the-connecttoloadbalancer-function-1",children:["The ",(0,s.jsx)(t.code,{children:"connectToLoadbalancer"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["The first function we\u2019ll examine is ",(0,s.jsx)(t.code,{children:"connectToLoadbalancer"}),". Its purpose is straightforward: establish a connection between the backend service and the load balancer. The load balancer\u2019s host and port are hardcoded within the function and used to initiate the connection."]}),"\n",(0,s.jsx)(t.p,{children:"Once connected, the backend stores both the positive handle (for synchronous communication) and the negative handle (for asynchronous calls). To ensure robustness, the entire connection attempt is wrapped in a trap, so any failure results in a controlled error rather than an unhandled exception."}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:'connectToLoadbalancer:{@[{NLB::neg LB::hopen x}; `:localhost:1234; {show "Can\'t connect to Load Balancer-> ",x}]};\n'})}),"\n",(0,s.jsxs)(t.h4,{id:"the-servicedetails-list",children:["The ",(0,s.jsx)(t.code,{children:"serviceDetails"})," List"]}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.code,{children:"serviceDetails"})," list acts as a parse tree that is sent to the load balancer immediately after a connection has been established. Its first element is the symbolic name of the ",(0,s.jsx)(t.code,{children:"registerService"})," function, followed by the service name and the backend service\u2019s address as arguments. When transmitted, this structure invokes ",(0,s.jsx)(t.code,{children:"registerService"})," on the load balancer side, effectively registering the backend service and making it known to the system."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:'serviceDetails:(`registerService; serviceName; `$":" sv string (();.z.h;system"p"));\n'})}),"\n",(0,s.jsxs)(t.h4,{id:"the-execquery-function",children:["The ",(0,s.jsx)(t.code,{children:"execQuery"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["The function that ultimately executes the client request on the backend is ",(0,s.jsx)(t.code,{children:"execQuery"}),". It takes two parameters. The first is the (negative) handle of the gateway that submitted the query, this handle is used to send the result back once processing is complete. The second parameter is a tuple, a two-element list: the sequence number of the query and the actual client query to be executed. The sequence number allows the gateway to correctly reconcile the response with the original request when it is returned."]}),"\n",(0,s.jsxs)(t.p,{children:["The client query itself is executed inside an error trap on the backend, ensuring that any runtime issues are safely contained. Once execution finishes, whether successfully or with an error, the result is sent back to the gateway by invoking ",(0,s.jsx)(t.code,{children:"returnRes"}),", which will then forward the response to the client."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"execQuery:{[nh;rq] nh(`returnRes;(rq 0;@[value;rq 1;{x}]));nh[]};\n"})}),"\n",(0,s.jsxs)(t.h4,{id:"the-queryservice-function",children:["The ",(0,s.jsx)(t.code,{children:"queryService"})," Function"]}),"\n",(0,s.jsxs)(t.p,{children:["Before reaching its final destination in ",(0,s.jsx)(t.code,{children:"execQuery"}),", he request is first routed through ",(0,s.jsx)(t.code,{children:"queryService"}),". This is the function invoked by the gateway, and it receives a tuple containing the query\u2019s sequence number and the actual client query."]}),"\n",(0,s.jsxs)(t.p,{children:["Rather than calling ",(0,s.jsx)(t.code,{children:"execQuery"})," directly, we first construct a projection that wraps the execution logic with proper error handling. This ensures that any exception raised during execution is safely captured and controlled. We then invoke",(0,s.jsx)(t.code,{children:"execQuery"})," and allow the query lifecycle to continue."]}),"\n",(0,s.jsxs)(t.p,{children:["Regardless of whether the query completes successfully or fails with an error, the final step inside ",(0,s.jsx)(t.code,{children:"queryService"})," is to release the backend service. Once execution finishes, the service is no longer blocked and can be returned to the pool for the next request."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"queryService:{\n  errProj:{[nh;sq;er]nh(sq;`$er);nh[]};\n  @[execQuery[neg .z.w];x;errProj[neg .z.w;x 0]];\n  NLB(`releaseService;serviceName) };\n"})}),"\n",(0,s.jsx)(t.h4,{id:"backend-bootstrapping-and-bureaucracy-wiring-the-service-to-the-grid",children:"Backend Bootstrapping and Bureaucracy: Wiring the Service to the Grid"}),"\n",(0,s.jsx)(t.p,{children:"With the core logic in place, all that remains is a bit of framework housekeeping."}),"\n",(0,s.jsxs)(t.p,{children:["We start by defining the ",(0,s.jsx)(t.strong,{children:(0,s.jsx)(t.code,{children:".z.ts"})})," function: the routine executed on each timer tick. Inside it, the backend attempts to establish a connection to the load balancer via ",(0,s.jsx)(t.code,{children:"connectToLoadBalancer"}),". If the connection succeeds, the service registers itself by sending the ",(0,s.jsx)(t.code,{children:"serviceDetails"})," parse tree to the load balancer. Once registration is complete and the backend is officially online, we reset the timer to zero, no further reconnection attempts are needed."]}),"\n",(0,s.jsxs)(t.p,{children:["For resilience, we also override ",(0,s.jsx)(t.strong,{children:(0,s.jsx)(t.code,{children:".z.pc"})}),", the handler triggered whenever a process disconnects from the backend. If the disconnected process happens to be the load balancer, we react by resetting the timer to a ten-second interval and repeatedly attempt to reconnect. This ensures the backend can recover automatically from infrastructure hiccups."]}),"\n",(0,s.jsxs)(t.p,{children:["Finally, we initialise some dummy ",(0,s.jsx)(t.strong,{children:(0,s.jsx)(t.code,{children:"trade"})})," and ",(0,s.jsx)(t.strong,{children:(0,s.jsx)(t.code,{children:"quote"})})," data to simulate our equity RDB and invoke ",(0,s.jsx)(t.code,{children:".z.ts"})," to kick everything off. With that, the backend service is live, registered, and ready to handle incoming client queries."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:'.z.ts:{connectToLoadbalancer[];if[0<LB;@[NLB;serviceDetails;{show x}];value"\\\\t 0"]};\n\nz.pc:{[handle]if[handle~LB;LB::0;value"\\\\t 10000"]};\n\nquote:([] date:10#.z.D-1; sym:10#`APPL; time:09:30t+00:30t*til 10; bid:100.+0.01*til 10; ask:101.+0.01*til 10)\ntrade:([] date:10#.z.D-1; sym:10#`AAPL; time:09:30t+00:30t*til 10; price:100.+0.01*til 10; size:10#100)\n\n.z.ts[]\n'})}),"\n",(0,s.jsx)(t.h3,{id:"client-onboarding-injecting-queries-into-the-machine",children:"Client Onboarding: Injecting Queries into the Machine"}),"\n",(0,s.jsx)(t.p,{children:"With the gateway, load balancer, and backend service all up and running, it\u2019s time to simulate a client and push some queries through the framework."}),"\n",(0,s.jsxs)(t.p,{children:["We spin up a fresh KDB/Q process and define a helper function that allows us to submit queries to the gateway. More specifically, we create a projection that accepts two inputs: the target service name and the query to be executed. The projection establishes a connection to the gateway using the supplied connection details, then constructs a lambda that invokes the ",(0,s.jsx)(t.code,{children:"getData"})," function asynchronously. After sending the request, it flushes the output queue to ensure the message is dispatched immediately."]}),"\n",(0,s.jsx)(t.p,{children:"This implements a deferred synchronous pattern: the client blocks while waiting for the result, but the gateway does not. That distinction is critical. The gateway must remain free to handle other incoming requests, because a gateway that blocks on the first client query isn\u2019t much of a gateway at all."}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:'q)gw:{h:hopen x;{(neg x)(`getData;y);x[]}[h]}[`:localhost:5555]\nq)gw(`EQUITY_MARKET_RDB;"select from trade")\ndate       sym  time         price  size\n----------------------------------------\n2026.02.14 AAPL 09:30:00.000 100    100\n2026.02.14 AAPL 10:00:00.000 100.01 100\n2026.02.14 AAPL 10:30:00.000 100.02 100\n2026.02.14 AAPL 11:00:00.000 100.03 100\n2026.02.14 AAPL 11:30:00.000 100.04 100\n2026.02.14 AAPL 12:00:00.000 100.05 100\n2026.02.14 AAPL 12:30:00.000 100.06 100\n2026.02.14 AAPL 13:00:00.000 100.07 100\n2026.02.14 AAPL 13:30:00.000 100.08 100\n2026.02.14 AAPL 14:00:00.000 100.09 100\n'})}),"\n",(0,s.jsx)(t.h2,{id:"lights-on-watching-the-framework-come-alive",children:"Lights On: Watching the Framework Come Alive"}),"\n",(0,s.jsx)(t.p,{children:"With the line-by-line walkthrough behind us, it\u2019s time to see the architecture in motion. Let\u2019s run a short demonstration of the system we\u2019ve just built and watch the components interact in real time. The illustration below highlights the full setup and how the pieces fit together. If you\u2019d like to recreate the framework yourself, you can find the complete codebase on DefconQ\u2019s GitHub repository."}),"\n",(0,s.jsx)(t.p,{children:(0,s.jsx)(t.img,{alt:"DefconQ Gateway-Loadbalancer Walkthrough",src:n(27256).A+"",width:"3412",height:"2142"})}),"\n",(0,s.jsx)(t.h2,{id:"where-do-we-go-from-here-scaling-the-blueprint",children:"Where Do We Go From Here? Scaling the Blueprint"}),"\n",(0,s.jsx)(t.p,{children:"This tutorial walked through a lightweight gateway-load balancer framework, breaking down the functionality and code step by step. By now, you should have a solid set of foundational building blocks, enough to evolve this simple architecture into something far more powerful."}),"\n",(0,s.jsx)(t.p,{children:"A natural next step would be to replace the deferred synchronous pattern with the deferred response pattern introduced in KDB/Q v3.6. From there, you could expand the ecosystem: add additional backend services, refine the load balancer\u2019s routing logic, introduce user-level permissions, or deploy multiple gateways segmented by region or asset class.\nThe framework is intentionally minimal, the ceiling is defined only by your imagination."}),"\n",(0,s.jsx)(t.p,{children:"As always, happy coding."}),"\n",(0,s.jsx)(t.h2,{id:"resources",children:"Resources"}),"\n",(0,s.jsxs)(t.ul,{children:["\n",(0,s.jsx)(t.li,{children:(0,s.jsx)(t.a,{href:"https://www.defconq.tech/docs/architecture/gateway",children:"Gateways: The Bouncer at the Front Door of Your KDB/Q Stack"})}),"\n",(0,s.jsx)(t.li,{children:(0,s.jsx)(t.a,{href:"https://www.defconq.tech/docs/architecture/advanced",children:"Advanced KDB/Q Architecture"})}),"\n",(0,s.jsx)(t.li,{children:(0,s.jsx)(t.a,{href:"https://www.defconq.tech/docs/concepts/ipc",children:"Fundamentals of Interprocess Communication (IPC)"})}),"\n",(0,s.jsx)(t.li,{children:(0,s.jsx)(t.a,{href:"https://www.defconq.tech/docs/concepts/ipcNextLevel",children:"Beyond the Fundamentals: Next Level Interprocess Communications"})}),"\n",(0,s.jsx)(t.li,{children:(0,s.jsx)(t.a,{href:"https://www.defconq.tech/docs/tutorials/tick",children:"KDB Tick Explained: A Walkthrough"})}),"\n",(0,s.jsx)(t.li,{children:(0,s.jsx)(t.a,{href:"https://code.kx.com/q/wp/query-routing/",children:"KX Whitepaper: Query Routing: A kdb+ framework for a scalable, load balanced system by Kevin Holsgrove"})}),"\n"]})]})}function d(e={}){const{wrapper:t}={...(0,i.R)(),...e.components};return t?(0,s.jsx)(t,{...e,children:(0,s.jsx)(l,{...e})}):l(e)}},27256:(e,t,n)=>{n.d(t,{A:()=>s});const s=n.p+"assets/images/GatewayDemo-17598c1b34755706ba2d304a5c0536f9.gif"},64892:(e,t,n)=>{n.d(t,{A:()=>s});const s=n.p+"assets/images/gatewayArchitecture-da0a3eedc39aa7e47025f8382d27cc3e.mov"},82946:(e,t,n)=>{n.d(t,{A:()=>s});const s=n.p+"assets/images/gatewayBouncerMind-17883e0b4d7e9e6b61031ff5b7628f06.png"},28453:(e,t,n)=>{n.d(t,{R:()=>r,x:()=>c});var s=n(96540);const i={},a=s.createContext(i);function r(e){const t=s.useContext(a);return s.useMemo((function(){return"function"==typeof e?e(t):{...t,...e}}),[t,e])}function c(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:r(e.components),s.createElement(a.Provider,{value:t},e.children)}}}]);